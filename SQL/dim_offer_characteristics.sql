CREATE TABLE dim_offer_characteristics (
    pk_offer_characteristics_id TINYINT PRIMARY KEY,
    car_garage VARCHAR(MAX),
    heating VARCHAR(MAX),
    lift VARCHAR(MAX),
    furniture VARCHAR(MAX)
);